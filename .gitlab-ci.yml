image: node:16

cache:
  paths:
    - node_modules/

before_script:
  - npm install

pages:
  script:
    - npm run build
    - cp -r dist public
  artifacts:
    paths:
      - public
  only:
    - main
